[package]
name = "rate-limiter-client"
version = "0.1.0"
edition = "2021"

[dependencies]
solana-onchain-rate-limiter = { path = "../program" }
borsh = { version = "1.5.1", features = ["derive"] }
